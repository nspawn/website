{
  "name": "nspawn-website",
  "version": "0.17.0",
  "description": "nspawn.org: documentation and landing page for nspawn, built with Hugo and Docsy.",
  "repository": "github:nspawn/nspawn.org",
  "homepage": "https://nspawn.org",
  "author": "The nspawn.org team",
  "license": "Apache-2.0",
  "bugs": "https://github.com/nspawn/nspawn.org/issues",
  "workspaces": [
    "packages/hugoautogen"
  ],
  "scripts": {
    "_build": "npm run _hugo-dev --",
    "_check:format": "prettier --check .",
    "_check:links": "lychee-norm-cache",
    "_diff:check": "git diff --name-only --exit-code",
    "_get:docsy": "hugo mod get github.com/google/docsy/theme@v$npm_package_version",
    "_hugo-dev": "npm run _hugo -- -e dev --logLevel info -DFE",
    "_hugo": "hugo --cleanDestinationDir",
    "_is:clean": "bash -c 'o=$(git status --porcelain -uall) && { echo \"$o\"; [ -z \"$o\" ]; }'",
    "_local": "cross-env HUGO_MODULE_WORKSPACE=docsy.work",
    "_serve": "npm run _hugo-dev -- --minify serve --renderToMemory",
    "_update:docsy:post": "hugo mod tidy && npm run update:docsy:pack",
    "approve:hugo": "npm approve-scripts hugo-extended && npm run update:docsy:pack",
    "build:preview": "npm run _hugo-dev -- --minify",
    "build:production": "npm run _hugo -- --minify",
    "build": "npm run _build -- ",
    "check:links:internal": "npm run build && npm run _check:links -- --offline",
    "check:links": "npm run build && npm run _check:links",
    "ci:prepare": "npm run _get:docsy && hugo mod graph && hugo mod tidy",
    "ci:test": "npm run ci:prepare && npm run fix:format && npm run test && npm run _diff:check",
    "clean": "rm -Rf public/* resources",
    "fix:format:diff": "npm run fix:format",
    "fix:format": "npm run _check:format -- --write",
    "fix": "npm run fix:format",
    "install:safe": "npm ci --ignore-scripts",
    "local": "npm run _local -- npm run",
    "seq": "bash -c 'for cmd in \"$@\"; do npm run $cmd || exit 1; done' - ",
    "serve": "npm run _serve",
    "serve:container": "npm run _serve -- --bind 0.0.0.0 --port 1313",
    "test-and-fix": "npm run fix && npm run test-only",
    "test-only": "npm run check:links && npm run test:site",
    "test:site": "node --test 'tests/**/*.test.mjs'",
    "test": "npm run test-and-fix",
    "update:docsy:main": "hugo mod get -u github.com/google/docsy/theme@main && npm run _update:docsy:post",
    "update:docsy:mod": "hugo mod get -u github.com/google/docsy/theme && npm run _update:docsy:post",
    "update:docsy:pack": "hugo mod npm pack && npm pkg set 'name=\"@docsy/hugoautogen\"' private=true --json --prefix packages/hugoautogen && npm install --ignore-scripts",
    "update:hugo": "npm install --save-dev --save-exact --ignore-scripts hugo-extended@latest",
    "update:main": "npm run update:packages && npm run update:docsy:main",
    "update:packages": "npm-check-updates -u -x hugo-extended",
    "update": "npm run update:packages && npm run update:docsy:mod"
  },
  "devDependencies": {
    "autoprefixer": "10.6.1",
    "cross-env": "10.1.0",
    "hugo-extended": "0.164.0",
    "link-cache": "0.5.0",
    "postcss-cli": "12.0.0",
    "rtlcss": "4.3.0",
    "sass-embedded": "1.104.1"
  },
  "optionalDependencies": {
    "npm-check-updates": "23.1.0",
    "prettier": "3.9.8"
  },
  "overrides": {
    "adm-zip": "0.6.0"
  },
  "engines": {
    "node": ">=24",
    "npm": ">=11.16.0"
  },
  "allowScripts": {
    "@parcel/watcher": false,
    "fsevents": false,
    "hugo-extended@0.164.0": true
  },
  "private": true,
  "prettier": {
    "proseWrap": "always",
    "singleQuote": true
  }
}
